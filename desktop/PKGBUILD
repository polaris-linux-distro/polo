pkgname=polo-desktop
pkgver=1.0
pkgrel=1
pkgdesc="Polaris desktop configs"
arch=('any')
license=('GPL')
source=('files-desktop')
md5sums=('SKIP')

package() {
    cd "$srcdir/files-desktop"
    find . -type f -exec install -Dm644 {} "$pkgdir/{}" \;
}